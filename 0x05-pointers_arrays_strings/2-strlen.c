#include "holberton.h"
/**
 * _strlen - check the code for Holberton School students.
 *
 * Return: Always 0.
 *
 *@s: ptr w string
 */

int _strlen(char *s)
{

int len;

for (len = 0; s[len] != '\0'; len++)
{
}
return (len);
}
