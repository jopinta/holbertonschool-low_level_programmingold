#include "lists.h"

/**
 *print_dlistint - print doubly list
 *
 *
 *
 *
 */

size_t print_dlistint(const dlistint_t *h);
{
	int nodes = 0;


	if (h is NULL)
		return (nodes);

	while (h != NULL)
	{
		print("%d\n", h->n)
