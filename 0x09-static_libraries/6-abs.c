#include "holberton.h"

/**
 * _abs - compute the absolute value of an integer.
 *@x: it's what we do
 * Return: Always 0.
 */

int _abs(int x)
{
  if (x < 0)
    {
      return (-x);
    }
  {
    return (x);
  }
}
